# OrangeAdmin 权限（RBAC）测试手册

> 适用版本：当前最新 build（预览地址 **http://localhost:4180/**）
> 演示账号：`admin/123456`（超级管理员）、`user/123456`（普通用户）
> ⚠️ 每次改完代码重新构建后，请对预览页 **Ctrl+F5 硬刷**，否则会加载旧包。

---

## 测试前置

1. 打开预览 `http://localhost:4180/`，用 `admin/123456` 登录。
2. 若之前测试把权限改乱了，可执行「一键复位」：
   **系统管理 → 角色管理 → 右上角「恢复默认数据」→ 确认 → 等待自动刷新**。
   这会清空 localStorage 里的角色权限覆盖，让超级管理员回到 `['*']`、普通用户回到 view 集合。

---

## 模块一：登录与基础可见性

### T-01 超级管理员登录
- 步骤：访问预览 → 登录 `admin/123456`
- 预期：
  - 自动进入「仪表盘」。
  - 侧边栏显示**全部菜单**：仪表盘 / 系统管理（用户、角色、菜单、部门、字典管理）/ 组件演示（表格、表单、图表）。
  - 进入任一列表页（如用户管理），**「新增 / 编辑 / 删除 / 导出」按钮都可见**（超级管理员 `*` 通配）。

### T-02 普通用户登录
- 步骤：退出 → 登录 `user/123456`
- 预期：
  - 同样自动进入仪表盘，侧边栏菜单与 admin 一致（种子给了各模块 view 权限）。
  - 进入「用户管理」页：**看不到「新增 / 删除」按钮**（种子只给了 `user:view`，没给操作权限）。
  - 进入「角色管理」页：**看不到「分配权限」按钮**（普通用户没有 `role:assign`）。

---

## 模块二：超级管理员权限回收（即时生效）

### T-03 回收「组件演示」→ 侧边栏立即隐藏
- 步骤：admin 登录 → 系统管理 → 角色管理 → 超级管理员行「分配权限」→ 取消「组件演示」下 **表格 / 表单 / 图表** 的全部勾选 → 点「确定」。
- 预期：
  - **侧边栏「组件演示」分组立刻消失**（本轮修复后保存即生效，无需刷新）。
  - 再次打开「分配权限」弹窗，取消状态应保持（持久化生效）。

### T-04 超级管理员自愈边界（了解即可，不必硬测）
- 说明：如果把超级管理员设成「纯查看、无任何操作权限」，系统会判定为旧数据污染、自动恢复 `['*']`（这是设计兜底，超级管理员本就该全权）。
- 正常取消**部分**权限（仍保留一些操作授权，如保留 `user:add`）不会被劫持 —— T-03 已间接验证。

---

## 模块三：普通用户精细化授权

### T-05 回收普通用户的「组件演示」
- 步骤：角色管理 → 普通用户「分配权限」→ 取消「组件演示」全部 → 确定 → 退出 → 登录 `user/123456`。
- 预期：普通用户侧边栏**不再显示「组件演示」分组**。

### T-06 给普通用户追加操作权限
- 步骤：角色管理 → 普通用户「分配权限」→ 勾选「用户管理 / 新增」「用户管理 / 编辑」→ 确定 → 登录 `user` → 进「用户管理」。
- 预期：用户管理页出现「新增」「编辑」按钮，但「删除」「导出」仍不可见（未授权）。

---

## 模块四：菜单管理联动（新增菜单自动进权限树）

### T-07 新增菜单出现在权限分配树
- 步骤：
  1. 系统管理 → 菜单管理 → 新增。
  2. 父级菜单选「系统管理」；标题随便（如「测试菜单」）；路由名 `demo`；路径 `/system/demo`；组件 `system/Demo`；权限标识留空或填 `demo:view` → 确定。
  3. 角色管理 → 任意角色「分配权限」。
- 预期：权限分配树里「系统管理」下**自动出现「测试菜单」**；勾选后该菜单可在侧边栏正常访问。

---

## 模块五：路由守卫（直接 URL 越权访问）

### T-08 无权限路由被拦截到 403
- 步骤：用 `user` 登录（已按 T-05 收回图表权限）→ 浏览器地址栏**直接输入** `http://localhost:4180/components/chart` 回车。
- 预期：被路由守卫拦截，跳转到 **403 页面**（而不是进入图表页）。有 `chart:view` 权限的 admin 直接输入该 URL 则正常进入。

---

## 模块六：边界与恢复 / 切换账号

### T-09 恢复默认数据
- 步骤：角色管理 → 「恢复默认数据」→ 确认 → 等待刷新。
- 预期：超级管理员变回 `['*']`、普通用户变回 view 集合；所有界面回到初始授权状态。

### T-10 切换账号陈旧状态
- 步骤：用 `admin` 登录（含全部菜单）→ 退出 → 登录 `user`。
- 预期：侧边栏**立即变为 user 的菜单**，不应残留 admin 才有的内容（切换账号会强制重注入路由与菜单）。

---

## 附：自动化验证（我这边已跑过）

项目内 `scripts/e2e_rbac.mjs` 复刻了完整 RBAC 链路（登录→拿角色→菜单过滤→按钮权限→路由守卫→分配持久化→权限树动态），**23 条断言全部 PASS**，并对比「修复前（含 menuPermissions 短路）/ 修复后」两套逻辑，直接证明了本轮修复生效：

- 修复前：回收角色权限后侧边栏仍显示菜单（bug 复现）。
- 修复后：回收即隐藏，普通用户精细化授权精确生效。

运行方式（可选自查）：
```bash
cd orange-admin
node scripts/e2e_rbac.mjs
```

其他专项脚本：`verify_user_perm.mjs`（hasPermission 真值表）、`verify_permtree.mjs`（权限树唯一 key）、`verify_rbac.mjs`（操作权限点建模）、`verify_menu_perm.mjs`（菜单过滤 + 切换账号）。
